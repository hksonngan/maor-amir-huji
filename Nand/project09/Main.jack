/**
 * The Main class initializes a new 4-in-a-row game and starts it.
 */
class Main {

    /** Initializes a new game and starts it. */    
    function void main() {
        var Game game;

        let game = Game.new();
        do game.run();
		do game.dispose();

        return;
    }
}
